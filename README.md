# SimpleTranslator (React Native + Expo)

This project now keeps only the React Native (Expo) implementation.

## Run

```bash
npm install
npm run start:clear
```

Then scan the QR code with Expo Go on your phone.
